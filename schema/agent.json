{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "handle": {
      "type": "string",
      "pattern": "^[a-zA-Z0-9][a-zA-Z0-9-]*$",
      "description": "The operator's handle for this agent (compose service, logs, CLI, session keys). Unless `name` is set, the agent chooses its own name on first boot."
    },
    "name": {
      "description": "Display name for the agent. When set, the agent skips the naming ritual and uses this name. Omit to let the agent choose its own name on first boot.",
      "type": "string",
      "minLength": 2,
      "maxLength": 40
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "description": "One line: what job this agent does. Also shown to the agent during the naming ritual."
    },
    "model": {
      "type": "object",
      "properties": {
        "provider": {
          "type": "string",
          "enum": [
            "openai-compatible",
            "anthropic",
            "gemini",
            "codex"
          ],
          "description": "LLM provider dialect. openai-compatible covers OpenAI, Ollama, vLLM, and any compatible proxy. gemini is the native Gemini API (generateContent). codex uses an OpenAI Codex (ChatGPT) subscription via the credentials from `codex login` (no API key)."
        },
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "Model identifier, e.g. gpt-5.4-mini or claude-sonnet-5."
        },
        "small": {
          "description": "Model for cheap internal calls (summaries, compaction, the naming ritual). Defaults to the main model.",
          "type": "string",
          "minLength": 1
        },
        "base_url": {
          "description": "Endpoint override, e.g. http://localhost:11434/v1 for Ollama with openai-compatible (no API key required with a base_url).",
          "type": "string",
          "format": "uri"
        },
        "api_key_env": {
          "description": "Name of the env var holding the API key — a reference, never a value. Defaults to OPENAI_API_KEY / ANTHROPIC_API_KEY / GEMINI_API_KEY per provider.",
          "type": "string",
          "minLength": 1
        },
        "fallbacks": {
          "description": "Model ids to fall back to when the primary fails.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "pricing": {
          "description": "What this model costs, in USD per million tokens, overriding the built-in price list. Set it when the model isn't in the list, when you're behind a proxy or a negotiated rate, or whenever limits.max_cost has to be exact — the built-in list is published list prices and can go stale.",
          "type": "object",
          "properties": {
            "input_per_mtok": {
              "type": "number",
              "minimum": 0,
              "description": "USD per million input tokens."
            },
            "output_per_mtok": {
              "type": "number",
              "minimum": 0,
              "description": "USD per million output tokens."
            }
          },
          "required": [
            "input_per_mtok",
            "output_per_mtok"
          ],
          "additionalProperties": false
        }
      },
      "required": [
        "provider",
        "id"
      ],
      "additionalProperties": false,
      "description": "Which model runs this agent, and how to reach it."
    },
    "purpose": {
      "type": "string",
      "minLength": 1,
      "description": "The agent's job description — what it does, how it behaves, when to stay quiet. Becomes the model's system prompt."
    },
    "triggers": {
      "description": "Event sources that wake the agent. With triggers, `af run` starts a long-lived service; without, an interactive REPL.",
      "type": "array",
      "items": {
        "oneOf": [
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "discord"
              },
              "channels": {
                "description": "Channel names or ids to listen in; omit for all channels the bot can see. DMs always pass.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "require_mention": {
                "description": "Only respond when the bot is @-mentioned. DMs always address the bot.",
                "type": "boolean"
              },
              "token_env": {
                "default": "DISCORD_BOT_TOKEN",
                "description": "Env var holding the Discord bot token.",
                "type": "string",
                "minLength": 1
              },
              "from_users": {
                "description": "Only handle messages from these authors (user ids or usernames); the filter runs before the model is called. Omit for anyone.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "reply_channel": {
                "description": "Channel id to post replies into instead of the source channel. Out-of-channel replies quote the triggering message and link back.",
                "type": "string",
                "minLength": 1
              },
              "allow_silence": {
                "default": false,
                "description": "Post nothing when the agent replies with __NO_REPLY__ (or nothing); punctuation and whitespace around the sentinel are tolerated. Instruct the sentinel in purpose.",
                "type": "boolean"
              },
              "show_typing": {
                "default": false,
                "description": "Show the typing indicator in the source channel while the agent works. Looks odd with allow_silence: typing may end in no message.",
                "type": "boolean"
              },
              "voice_channels": {
                "description": "Voice channel names or ids to join for live spoken conversations. Requires the top-level voice.live block; the bot joins at startup and stays. Live voice media rides UDP, which keeps the agent out of hermetic mode — the container is its egress boundary.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": false,
            "description": "Listen to Discord messages via the gateway; replies go in-channel by default."
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "slack"
              },
              "transport": {
                "default": "socket",
                "description": "How events arrive. socket holds a Socket Mode connection open (no public endpoint, needs an always-on process). events_api serves Slack's Events API over inbound HTTP, so the host can scale to zero between messages — but needs a public HTTPS endpoint (the request URL in the Slack app config).",
                "type": "string",
                "enum": [
                  "socket",
                  "events_api"
                ]
              },
              "channels": {
                "description": "Channel names or ids to listen in; omit for all channels the bot is in.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "require_mention": {
                "description": "Only respond when the bot is @-mentioned. DMs always address the bot.",
                "type": "boolean"
              },
              "token_env": {
                "default": "SLACK_BOT_TOKEN",
                "description": "Env var holding the bot token (xoxb-…) — reads channel info, posts replies.",
                "type": "string",
                "minLength": 1
              },
              "app_token_env": {
                "default": "SLACK_APP_TOKEN",
                "description": "Env var holding the app-level token (xapp-…, scope connections:write). Only the socket transport needs it.",
                "type": "string",
                "minLength": 1
              },
              "port": {
                "default": 8080,
                "description": "events_api transport: port to listen on.",
                "type": "integer",
                "minimum": 1,
                "maximum": 65535
              },
              "path": {
                "default": "/slack",
                "description": "events_api transport: HTTP path Slack POSTs event deliveries to.",
                "type": "string",
                "pattern": "^\\/.*"
              },
              "signing_secret_env": {
                "default": "SLACK_SIGNING_SECRET",
                "description": "events_api transport: env var holding the app's signing secret. Every delivery's X-Slack-Signature is verified before parsing; unsigned POSTs get a 401.",
                "type": "string",
                "minLength": 1
              },
              "from_users": {
                "description": "Only handle messages from these Slack user ids (U…); the filter runs before the model is called. Omit for anyone.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "reply_channel": {
                "description": "Channel id to post replies into instead of the source thread. Out-of-channel replies quote the triggering message and link back.",
                "type": "string",
                "minLength": 1
              },
              "allow_silence": {
                "default": false,
                "description": "Post nothing when the agent replies with exactly __NO_REPLY__ (or nothing). Instruct the sentinel in purpose.",
                "type": "boolean"
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": false,
            "description": "Listen to Slack messages via Socket Mode (default, no public endpoint) or the Events API (inbound HTTP, scale-to-zero friendly); replies go in-thread by default."
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "telegram"
              },
              "transport": {
                "default": "polling",
                "description": "How updates arrive. polling long-polls getUpdates (no public endpoint, needs an always-on process). webhook registers public_url + path via setWebhook and serves inbound HTTP, so the host can scale to zero between messages — Telegram queues undelivered updates (~24h) and retries, which is what wakes the host back up.",
                "type": "string",
                "enum": [
                  "polling",
                  "webhook"
                ]
              },
              "chats": {
                "description": "Chat ids, group titles, or public @usernames to listen in; omit for all chats the bot sees.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "require_mention": {
                "description": "Only respond when the bot is @-mentioned. Private chats always address the bot.",
                "type": "boolean"
              },
              "token_env": {
                "default": "TELEGRAM_BOT_TOKEN",
                "description": "Env var holding the bot token from @BotFather.",
                "type": "string",
                "minLength": 1
              },
              "from_users": {
                "description": "Only handle messages from these authors (user ids or usernames); the filter runs before the model is called. Omit for anyone.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "reply_chat": {
                "description": "Chat id to post replies into instead of the source chat. Out-of-chat replies quote the triggering message and link back.",
                "type": "string",
                "minLength": 1
              },
              "allow_silence": {
                "default": false,
                "description": "Post nothing when the agent replies with exactly __NO_REPLY__ (or nothing). Instruct the sentinel in purpose.",
                "type": "boolean"
              },
              "port": {
                "default": 8080,
                "description": "webhook transport: port to listen on.",
                "type": "integer",
                "minimum": 1,
                "maximum": 65535
              },
              "path": {
                "default": "/telegram",
                "description": "webhook transport: HTTP path Telegram POSTs updates to.",
                "type": "string",
                "pattern": "^\\/.*"
              },
              "public_url": {
                "description": "webhook transport: the externally reachable HTTPS base registered via setWebhook (e.g. https://my-agent.fly.dev). Required when transport is webhook.",
                "type": "string",
                "format": "uri"
              }
            },
            "required": [
              "type"
            ],
            "additionalProperties": false,
            "description": "Listen to Telegram messages via Bot API long-polling (default, no public endpoint) or a registered webhook (inbound HTTP, scale-to-zero friendly); replies go in-chat by default."
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "webhook"
              },
              "path": {
                "default": "/",
                "description": "HTTP path to serve.",
                "type": "string",
                "pattern": "^\\/.*"
              },
              "port": {
                "default": 8080,
                "description": "Port to listen on.",
                "type": "integer",
                "minimum": 1,
                "maximum": 65535
              },
              "token_env": {
                "type": "string",
                "minLength": 1,
                "description": "Env var holding the bearer token callers must present. Required: an unauthenticated endpoint contradicts deny-by-default."
              }
            },
            "required": [
              "type",
              "token_env"
            ],
            "additionalProperties": false,
            "description": "HTTP trigger: POST {path} with authorization: Bearer <token> and JSON body {input, conversation_id?}; responds with the run result."
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "tty"
              },
              "path": {
                "default": "/tty",
                "description": "HTTP path that accepts the WebSocket upgrade.",
                "type": "string",
                "pattern": "^\\/.*"
              },
              "port": {
                "default": 8090,
                "description": "Port to listen on.",
                "type": "integer",
                "minimum": 1,
                "maximum": 65535
              },
              "token_env": {
                "type": "string",
                "minLength": 1,
                "description": "Env var holding the bearer token clients must present (authorization header, or the Sec-WebSocket-Protocol value `bearer.<token>` from browsers). Required: an unauthenticated terminal contradicts deny-by-default."
              }
            },
            "required": [
              "type",
              "token_env"
            ],
            "additionalProperties": false,
            "description": "Interactive terminal over WebSocket: connect to {path}, send {type: 'input', text} frames and receive the run's progress and result as JSON frames. This is the REPL as a network surface, for hosted terminals."
          },
          {
            "oneOf": [
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "email"
                  },
                  "transport": {
                    "type": "string",
                    "const": "resend",
                    "description": "How mail arrives. resend is the pushed transport: Resend receives mail for your domain and POSTs each message here as a signed webhook."
                  },
                  "path": {
                    "default": "/email",
                    "description": "HTTP path the provider POSTs inbound-mail webhooks to.",
                    "type": "string",
                    "pattern": "^\\/.*"
                  },
                  "port": {
                    "default": 8080,
                    "description": "Port to listen on.",
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 65535
                  },
                  "signing_secret_env": {
                    "default": "RESEND_WEBHOOK_SECRET",
                    "description": "Env var holding the webhook signing secret (whsec_…). Every request is verified before parsing; unsigned POSTs get a 401.",
                    "type": "string",
                    "minLength": 1
                  },
                  "api_key_env": {
                    "default": "RESEND_API_KEY",
                    "description": "Env var holding the Resend API key — fetches message bodies and sends replies.",
                    "type": "string",
                    "minLength": 1
                  },
                  "from_addresses": {
                    "minItems": 1,
                    "type": "array",
                    "items": {
                      "type": "string",
                      "minLength": 1
                    },
                    "description": "Senders the agent handles: exact addresses or *@domain patterns. Required — an email address is open to the whole internet. [\"*\"] accepts anyone, stated in the file that defines the agent's reach."
                  },
                  "allow_silence": {
                    "default": false,
                    "description": "Send nothing when the agent replies with exactly __NO_REPLY__ (or nothing). Instruct the sentinel in purpose.",
                    "type": "boolean"
                  }
                },
                "required": [
                  "type",
                  "transport",
                  "from_addresses"
                ],
                "additionalProperties": false,
                "description": "Wake on inbound email delivered by Resend's email.received webhook; replies go back out through the Resend API in the same thread."
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "email"
                  },
                  "transport": {
                    "type": "string",
                    "const": "imap",
                    "description": "How mail arrives. imap is the pulled transport for an existing mailbox: the trigger polls the folder for unseen messages and replies over SMTP."
                  },
                  "host": {
                    "type": "string",
                    "minLength": 1,
                    "description": "IMAP server host, e.g. imap.fastmail.com."
                  },
                  "port": {
                    "default": 993,
                    "description": "IMAP port (implicit TLS).",
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 65535
                  },
                  "username": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Mailbox login — also the address replies are sent from."
                  },
                  "password_env": {
                    "default": "IMAP_PASSWORD",
                    "description": "Env var holding the mailbox password (an app password where the provider requires one).",
                    "type": "string",
                    "minLength": 1
                  },
                  "smtp_host": {
                    "type": "string",
                    "minLength": 1,
                    "description": "SMTP server for replies, e.g. smtp.fastmail.com."
                  },
                  "smtp_port": {
                    "default": 465,
                    "description": "SMTP port (implicit TLS).",
                    "type": "integer",
                    "minimum": 1,
                    "maximum": 65535
                  },
                  "folder": {
                    "default": "INBOX",
                    "description": "Folder to watch. The unseen flag is the cursor — handled and dropped messages are marked seen — so give the agent a folder it owns.",
                    "type": "string",
                    "minLength": 1
                  },
                  "poll_seconds": {
                    "default": 60,
                    "description": "Seconds between polls.",
                    "type": "integer",
                    "minimum": 5,
                    "maximum": 9007199254740991
                  },
                  "from_addresses": {
                    "minItems": 1,
                    "type": "array",
                    "items": {
                      "type": "string",
                      "minLength": 1
                    },
                    "description": "Senders the agent handles: exact addresses or *@domain patterns. Required. [\"*\"] accepts anyone."
                  },
                  "allow_silence": {
                    "default": false,
                    "description": "Send nothing when the agent replies with exactly __NO_REPLY__ (or nothing). Instruct the sentinel in purpose.",
                    "type": "boolean"
                  }
                },
                "required": [
                  "type",
                  "transport",
                  "host",
                  "username",
                  "smtp_host",
                  "from_addresses"
                ],
                "additionalProperties": false,
                "description": "Wake on unseen messages in an IMAP mailbox; replies go out over SMTP in the same thread."
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "email"
                  },
                  "transport": {
                    "type": "string",
                    "const": "gmail",
                    "description": "How mail arrives. gmail polls the Gmail API for unread messages in a label, authenticated by an OAuth refresh token."
                  },
                  "client_id": {
                    "type": "string",
                    "minLength": 1,
                    "description": "OAuth client id of your Google Cloud project's Desktop-app client."
                  },
                  "client_secret_env": {
                    "default": "GMAIL_CLIENT_SECRET",
                    "description": "Env var holding the OAuth client secret.",
                    "type": "string",
                    "minLength": 1
                  },
                  "refresh_token_env": {
                    "default": "GMAIL_REFRESH_TOKEN",
                    "description": "Env var holding the refresh token from the one-time consent flow (docs/email.mdx documents it).",
                    "type": "string",
                    "minLength": 1
                  },
                  "label": {
                    "default": "INBOX",
                    "description": "Gmail label to watch. The unread state is the cursor — handled and dropped messages are marked read — so route mail here with Gmail filters and let the agent own the label.",
                    "type": "string",
                    "minLength": 1
                  },
                  "poll_seconds": {
                    "default": 60,
                    "description": "Seconds between polls.",
                    "type": "integer",
                    "minimum": 5,
                    "maximum": 9007199254740991
                  },
                  "from_addresses": {
                    "minItems": 1,
                    "type": "array",
                    "items": {
                      "type": "string",
                      "minLength": 1
                    },
                    "description": "Senders the agent handles: exact addresses or *@domain patterns. Required. [\"*\"] accepts anyone."
                  },
                  "allow_silence": {
                    "default": false,
                    "description": "Send nothing when the agent replies with exactly __NO_REPLY__ (or nothing). Instruct the sentinel in purpose.",
                    "type": "boolean"
                  }
                },
                "required": [
                  "type",
                  "transport",
                  "client_id",
                  "from_addresses"
                ],
                "additionalProperties": false,
                "description": "Wake on unread Gmail messages in a label via the Gmail API; replies land in the same Gmail thread."
              },
              {
                "type": "object",
                "properties": {
                  "type": {
                    "type": "string",
                    "const": "email"
                  },
                  "transport": {
                    "type": "string",
                    "const": "outlook",
                    "description": "How mail arrives. outlook polls Microsoft Graph for unread messages in a folder, authenticated by an OAuth refresh token."
                  },
                  "client_id": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Client id of your Entra app registration."
                  },
                  "client_secret_env": {
                    "description": "Env var holding the client secret — confidential clients only. Public (device-code) clients omit this.",
                    "type": "string",
                    "minLength": 1
                  },
                  "refresh_token_env": {
                    "default": "OUTLOOK_REFRESH_TOKEN",
                    "description": "Env var holding the refresh token from the one-time device-code flow (docs/email.mdx documents it).",
                    "type": "string",
                    "minLength": 1
                  },
                  "tenant": {
                    "default": "common",
                    "description": "Entra tenant: \"common\", \"consumers\", \"organizations\" or a tenant id.",
                    "type": "string",
                    "minLength": 1
                  },
                  "folder": {
                    "default": "inbox",
                    "description": "Mail folder to watch: a well-known name (inbox) or a folder id. The unread state is the cursor — handled and dropped messages are marked read.",
                    "type": "string",
                    "minLength": 1
                  },
                  "poll_seconds": {
                    "default": 60,
                    "description": "Seconds between polls.",
                    "type": "integer",
                    "minimum": 5,
                    "maximum": 9007199254740991
                  },
                  "from_addresses": {
                    "minItems": 1,
                    "type": "array",
                    "items": {
                      "type": "string",
                      "minLength": 1
                    },
                    "description": "Senders the agent handles: exact addresses or *@domain patterns. Required. [\"*\"] accepts anyone."
                  },
                  "allow_silence": {
                    "default": false,
                    "description": "Send nothing when the agent replies with exactly __NO_REPLY__ (or nothing). Instruct the sentinel in purpose.",
                    "type": "boolean"
                  }
                },
                "required": [
                  "type",
                  "transport",
                  "client_id",
                  "from_addresses"
                ],
                "additionalProperties": false,
                "description": "Wake on unread Outlook messages in a folder via Microsoft Graph; replies land in the same conversation."
              }
            ],
            "description": "Wake on inbound email. transport picks how mail arrives: resend (pushed webhooks for the agent's own address), or imap / gmail / outlook (poll an existing mailbox)."
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "github"
              },
              "path": {
                "default": "/github",
                "description": "HTTP path GitHub POSTs webhook deliveries to.",
                "type": "string",
                "pattern": "^\\/.*"
              },
              "port": {
                "default": 8080,
                "description": "Port to listen on.",
                "type": "integer",
                "minimum": 1,
                "maximum": 65535
              },
              "secret_env": {
                "default": "GITHUB_WEBHOOK_SECRET",
                "description": "Env var holding the webhook secret. Every delivery's X-Hub-Signature-256 is verified before parsing; unsigned POSTs get a 401.",
                "type": "string",
                "minLength": 1
              },
              "events": {
                "minItems": 1,
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                },
                "description": "Events that wake the agent: an event name (\"pull_request\"), an event.action pair (\"pull_request.opened\"), or \"*\" for every event the webhook sends. Required — GitHub sends many event types down one hook."
              },
              "repos": {
                "description": "Repositories the agent handles: owner/repo or owner/* patterns. Omit to accept any repository the webhook covers (useful for org-level hooks).",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              }
            },
            "required": [
              "type",
              "events"
            ],
            "additionalProperties": false,
            "description": "Wake on GitHub webhook events (pull requests, issues, pushes, releases and the rest). No reply channel: the agent acts through its permissions, typically the gh CLI."
          },
          {
            "type": "object",
            "properties": {
              "type": {
                "type": "string",
                "const": "cron"
              },
              "schedule": {
                "type": "string",
                "minLength": 1,
                "description": "Cron expression, e.g. \"0 9 * * 1\" for Mondays 09:00."
              },
              "prompt": {
                "type": "string",
                "minLength": 1,
                "description": "What to tell the agent each tick."
              }
            },
            "required": [
              "type",
              "schedule",
              "prompt"
            ],
            "additionalProperties": false,
            "description": "Fire the agent on a schedule with a fixed prompt."
          }
        ],
        "description": "An event source that wakes the agent."
      }
    },
    "voice": {
      "type": "object",
      "properties": {
        "stt": {
          "type": "object",
          "properties": {
            "provider": {
              "type": "string",
              "enum": [
                "openai",
                "elevenlabs"
              ],
              "description": "Speech-to-text provider for incoming voice notes."
            },
            "model": {
              "description": "Transcription model. Defaults per provider: gpt-4o-mini-transcribe (openai), scribe_v2 (elevenlabs).",
              "type": "string",
              "minLength": 1
            },
            "api_key_env": {
              "description": "Name of the env var holding the API key — a reference, never a value. Defaults to OPENAI_API_KEY / ELEVENLABS_API_KEY per provider.",
              "type": "string",
              "minLength": 1
            },
            "base_url": {
              "description": "Endpoint override for the openai provider, e.g. a metering gateway. Ignored by elevenlabs.",
              "type": "string",
              "minLength": 1
            }
          },
          "required": [
            "provider"
          ],
          "additionalProperties": false,
          "description": "How incoming voice notes become text."
        },
        "tts": {
          "type": "object",
          "properties": {
            "provider": {
              "type": "string",
              "enum": [
                "openai",
                "elevenlabs"
              ],
              "description": "Text-to-speech provider for spoken replies."
            },
            "model": {
              "description": "Speech model. Defaults per provider: gpt-4o-mini-tts (openai), eleven_multilingual_v2 (elevenlabs).",
              "type": "string",
              "minLength": 1
            },
            "voice": {
              "description": "Voice to speak with: a voice name for openai (defaults to alloy), a voice id for elevenlabs (defaults to Rachel).",
              "type": "string",
              "minLength": 1
            },
            "api_key_env": {
              "description": "Name of the env var holding the API key — a reference, never a value. Defaults to OPENAI_API_KEY / ELEVENLABS_API_KEY per provider.",
              "type": "string",
              "minLength": 1
            },
            "base_url": {
              "description": "Endpoint override for the openai provider, e.g. a metering gateway. Ignored by elevenlabs.",
              "type": "string",
              "minLength": 1
            }
          },
          "required": [
            "provider"
          ],
          "additionalProperties": false,
          "description": "How replies to voice notes become speech. Omit this block and voice notes get text replies."
        },
        "live": {
          "type": "object",
          "properties": {
            "provider": {
              "type": "string",
              "const": "openai",
              "description": "Realtime speech-to-speech provider. openai is the only dialect today: the Realtime API now, the gpt-live models the day their API opens."
            },
            "model": {
              "default": "gpt-realtime-2.1",
              "description": "Realtime model that holds the live conversation.",
              "type": "string",
              "minLength": 1
            },
            "voice": {
              "default": "marin",
              "description": "Voice the session speaks with.",
              "type": "string",
              "minLength": 1
            },
            "api_key_env": {
              "description": "Name of the env var holding the API key — a reference, never a value. Defaults to OPENAI_API_KEY.",
              "type": "string",
              "minLength": 1
            },
            "idle_seconds": {
              "default": 60,
              "description": "Close the realtime session after this much silence; it reopens on the next voice activity. Live sessions bill by the audio minute, so idle time is money.",
              "type": "integer",
              "exclusiveMinimum": 0,
              "maximum": 9007199254740991
            }
          },
          "required": [
            "provider"
          ],
          "additionalProperties": false,
          "description": "Live voice in Discord voice channels (plan 15): a realtime model holds the conversation and delegates real work to the agent loop through a single tool. Pair with a discord trigger's voice_channels."
        }
      },
      "additionalProperties": false,
      "description": "Voice on chat triggers. stt transcribes an incoming voice note (telegram, discord) and the transcript runs through the normal text loop; with tts the reply comes back as a voice note too. live holds spoken conversations in Discord voice channels. Omit the block and voice notes are ignored."
    },
    "skills": {
      "description": "Paths to skill markdown files (relative to this config). Skills carry knowledge, never capability.",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "tools": {
      "description": "Tool sources beyond the natives and skills.",
      "type": "object",
      "properties": {
        "mcp": {
          "description": "MCP servers to connect at startup.",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "name": {
                "type": "string",
                "minLength": 1,
                "description": "Server name; its tools appear namespaced as mcp__<name>__<tool>."
              },
              "command": {
                "description": "stdio server: argv to launch it, e.g. [docker, run, -i, ghcr.io/github/github-mcp-server].",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "url": {
                "description": "HTTP server: the endpoint URL.",
                "type": "string",
                "format": "uri"
              },
              "env": {
                "description": "Env vars passed to the server — values may be ${VAR} references. The server sees only these (scoped, no ambient inheritance).",
                "type": "object",
                "propertyNames": {
                  "type": "string"
                },
                "additionalProperties": {
                  "type": "string"
                }
              },
              "include": {
                "description": "Expose only these tools. Strongly recommended: a fat server must not blow a small model's context.",
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1
                }
              },
              "readonly": {
                "description": "Expose only tools whose readOnlyHint annotation marks them read-only. A guard against wiring write tools into a read-only job; the hint is self-reported by the server.",
                "type": "boolean"
              }
            },
            "required": [
              "name"
            ],
            "additionalProperties": false,
            "description": "A Model Context Protocol server providing tools to the agent."
          }
        },
        "search": {
          "default": "auto",
          "description": "Tool search: defer MCP tool schemas out of context behind a search_tools tool. auto defers when the agent has more than 10 tools; on always defers; off loads everything.",
          "type": "string",
          "enum": [
            "auto",
            "on",
            "off"
          ]
        }
      },
      "additionalProperties": false
    },
    "commands": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "pattern": "^[a-z][a-z0-9_]{0,31}$",
            "description": "What operators type after the slash: /standup. Lowercase letters, digits, underscores, max 32 chars — valid on every chat platform."
          },
          "description": {
            "type": "string",
            "minLength": 3,
            "maxLength": 100,
            "description": "One line shown by /help and in each platform's native command picker (platforms cap this around 100 chars)."
          },
          "prompt": {
            "type": "string",
            "minLength": 1,
            "description": "The prompt the command runs through the normal agent loop. $ARGS is replaced with whatever follows the command name."
          }
        },
        "required": [
          "name",
          "description",
          "prompt"
        ],
        "additionalProperties": false,
        "description": "An operator-defined slash command: a named, repeatable prompt."
      },
      "description": "Operator-defined slash commands, alongside the built-ins /help, /status, /reset, /compact and /new."
    },
    "permissions": {
      "type": "object",
      "properties": {
        "net": {
          "description": "Hosts http_request may reach; *.example.com matches subdomains (not the apex). A bare * allows every host — the escape hatch for agents whose job is the open web, stated loudly enough to catch in review.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "run": {
          "description": "Executables run_bash may spawn, matched by basename. run_bash exists only if this grants something. A bare * allows every executable.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "read": {
          "description": "Readable path prefixes.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "write": {
          "description": "Writable path prefixes.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      },
      "additionalProperties": false,
      "description": "Deny-by-default allowlists. Omitting this block means the agent can touch nothing; denials are surfaced to the model as tool results."
    },
    "env": {
      "description": "Secret env for tools and MCP servers. Values may be ${VAR} references, resolved at startup (env var, then /run/secrets/<VAR>) and scoped per tool. Everything here is treated as a secret and redacted from tool results, logs and traces — use `public` for configuration the agent must be able to read.",
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {
        "type": "string"
      }
    },
    "public": {
      "description": "Non-secret env, scoped to tools exactly like `env` but never redacted. For configuration the agent has to see in its own output — a project id, an account number, a region. Redacting one of these scrubs it out of the URLs and responses the agent needs to read.",
      "type": "object",
      "propertyNames": {
        "type": "string"
      },
      "additionalProperties": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "number"
          },
          {
            "type": "boolean"
          }
        ]
      }
    },
    "http": {
      "type": "object",
      "properties": {
        "auth": {
          "description": "Credentials the runtime attaches to http_request calls, matched by URL prefix.",
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "url": {
                "type": "string",
                "minLength": 1,
                "description": "URL prefix this credential applies to, e.g. https://api.stripe.com. The longest matching prefix wins. The host must also be in permissions.net."
              },
              "header": {
                "default": "Authorization",
                "description": "Header to attach the credential as.",
                "type": "string",
                "minLength": 1
              },
              "value": {
                "type": "string",
                "minLength": 1,
                "description": "The header value, e.g. Bearer ${STRIPE_KEY}. ${VAR} references resolve at startup (env var, then /run/secrets/<VAR>) and are redacted from every output surface."
              }
            },
            "required": [
              "url",
              "value"
            ],
            "additionalProperties": false,
            "description": "One credential the runtime attaches to matching outbound requests."
          }
        }
      },
      "additionalProperties": false,
      "description": "Outbound HTTP the runtime handles on the agent's behalf. The model asks for a URL; the credential is attached after the tool call, so it never passes through the model's context."
    },
    "redact": {
      "type": "object",
      "properties": {
        "headers": {
          "description": "Extra header and field names whose value is scrubbed by name, on top of authorization, cookie, x-api-key and the other usual suspects.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "values": {
          "description": "Extra ${VAR} references to scrub — a secret the agent reaches that this config does not otherwise name, such as one baked into an MCP server's own image.",
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          }
        }
      },
      "additionalProperties": false,
      "description": "Additions to the redaction the framework already does. Every secret the config references is redacted without being listed here."
    },
    "memory": {
      "type": "object",
      "properties": {
        "scope": {
          "default": "none",
          "description": "thread: conversation history persists per conversation key (chat channel or thread, webhook conversation_id). none: every run starts fresh.",
          "type": "string",
          "enum": [
            "thread",
            "none"
          ]
        },
        "persistent": {
          "default": false,
          "description": "Give the agent remember/recall/list_memories/forget tools backed by its own SQLite file — facts and preferences that survive across conversation keys and restarts, not just one thread's history.",
          "type": "boolean"
        },
        "compact_at_tokens": {
          "default": 50000,
          "description": "Auto-compact a conversation once its context reaches this many tokens (the input tokens the provider reported for the run's last LLM call). The older history is replaced by a summary written via model.small; the most recent turns survive verbatim. false disables auto-compaction; without scope: thread there is no history, so the threshold is inert.",
          "anyOf": [
            {
              "type": "boolean",
              "const": false
            },
            {
              "type": "integer",
              "exclusiveMinimum": 0,
              "maximum": 9007199254740991
            }
          ]
        }
      },
      "additionalProperties": false,
      "description": "What the agent remembers between events."
    },
    "schedules": {
      "type": "object",
      "properties": {
        "max": {
          "default": 20,
          "description": "How many schedules may exist at once. The schedule tool refuses past the cap and tells the model to unschedule something first, so an unattended agent cannot accumulate unbounded future work.",
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        }
      },
      "additionalProperties": false,
      "description": "Let the agent schedule future runs of itself: a schedule/list_schedules/unschedule tool trio backed by its own SQLite file. Schedules survive restarts, fire through the normal run path, and deliver their result to the conversation that created them. Omit the block and the tools do not exist."
    },
    "limits": {
      "default": {
        "max_steps": 20,
        "concurrent_runs": 4,
        "queue_depth": 10,
        "max_image_bytes": 5000000,
        "max_images_per_message": 4,
        "max_cost": 5,
        "max_runtime": 0
      },
      "type": "object",
      "properties": {
        "max_steps": {
          "default": 20,
          "description": "Tool-calling iterations (LLM calls) per run. On hitting the cap the agent gets one final tool-less call to summarize its progress, then the run ends with error_max_steps.",
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "concurrent_runs": {
          "default": 4,
          "description": "Parallel runs across conversations. Within one conversation runs are always serial and ordered, so each run sees what earlier messages did; this caps how many conversations run at once. 1 serializes the whole agent.",
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "queue_depth": {
          "default": 10,
          "description": "Events that may wait per conversation while a run is in flight. An event past the cap is refused immediately with a short reply, and every refusal lands in the audit trail.",
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991
        },
        "max_image_bytes": {
          "default": 5000000,
          "description": "Largest image a channel may hand the model. A bigger one is named in the prompt and not read, so the agent can say what arrived and why it didn't look at it.",
          "type": "integer",
          "exclusiveMinimum": 0,
          "maximum": 9007199254740991
        },
        "max_images_per_message": {
          "default": 4,
          "description": "Images one message may carry into the context. An image costs thousands of input tokens, and a channel will happily deliver twenty. The rest are named and not read; 0 reads none.",
          "type": "integer",
          "minimum": 0,
          "maximum": 9007199254740991
        },
        "max_cost": {
          "default": 5,
          "description": "Most one run may spend on model calls, in USD. Checked before each call using what the run has already spent, so the cap is a ceiling on committed spend rather than a prediction. The run ends with error_max_cost. 0 disables the cap; there is a non-zero default because an unattended agent with no ceiling is one loop away from an unbounded bill.",
          "type": "number",
          "minimum": 0
        },
        "max_runtime": {
          "default": 0,
          "description": "Wall-clock seconds one run may take, measured from when it starts. Checked at step boundaries, so a single long tool call can overshoot it. The run ends with error_max_runtime. 0 disables it, which is the default: unlike spend, a slow run costs nothing extra and legitimate work can be slow.",
          "type": "number",
          "minimum": 0
        }
      },
      "additionalProperties": false,
      "description": "Per-run budgets — the dead-man's switches for unattended operation."
    }
  },
  "required": [
    "handle",
    "description",
    "model",
    "purpose"
  ],
  "additionalProperties": false,
  "description": "A Looped AF agent: one job, one file. https://github.com/loopedautomation/agent-framework"
}

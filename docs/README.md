# Documentation

The Looped AF documentation site. The scaffold lands in M1; content ships with every milestone — a feature PR that doesn't touch `docs/` isn't done (Plan 0, principle 10).

Target bar: a newcomer goes from zero to their own running agent in under 30 minutes.
